var options = {
    width: 300,
    height: 400,
    zoomWidth: 450,
    zoomPosition: "original",
};

var ImageZoom = new ImageZoom(document.getElementById("img-container"), options);

ImageZoom.setup();